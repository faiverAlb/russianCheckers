<Window x:Class="RussianCheckers.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RussianCheckers"
        mc:Ignorable="d"
        Title="Russian checkers" Height="700" Width="650" ResizeMode="NoResize" WindowStartupLocation="CenterScreen">

        <Window.Resources>
            <DrawingBrush x:Key="Checkerboard" Stretch="None" TileMode="Tile" Viewport="0,0,2,2" ViewportUnits="Absolute">
                <DrawingBrush.Drawing>
                    <DrawingGroup>
                        <GeometryDrawing Brush="Tan">
                            <GeometryDrawing.Geometry>
                                <RectangleGeometry Rect="0,0,2,2" />
                            </GeometryDrawing.Geometry>
                        </GeometryDrawing>
                        <GeometryDrawing Brush="Brown">
                            <GeometryDrawing.Geometry>
                                <GeometryGroup>
                                    <RectangleGeometry Rect="0,0,1,1" />
                                    <RectangleGeometry Rect="1,1,1,1" />
                                </GeometryGroup>
                            </GeometryDrawing.Geometry>
                        </GeometryDrawing>
                    </DrawingGroup>
                </DrawingBrush.Drawing>
            </DrawingBrush>

        <Style x:Key="ChessPieceStyle" TargetType="{x:Type Image}">
            <Style.Triggers>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding Type}" Value="{x:Static local:PieceType.Queen}"/>
                        <Condition Binding="{Binding Side}" Value="{x:Static local:Side.Black}"/>
                    </MultiDataTrigger.Conditions>
                    <MultiDataTrigger.Setters>
                        <Setter Property="Image.Source" Value="Images/black_queen.png" />
                    </MultiDataTrigger.Setters>
                </MultiDataTrigger>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding Type}" Value="{x:Static local:PieceType.Queen}"/>
                        <Condition Binding="{Binding Side}" Value="{x:Static local:Side.White}"/>
                    </MultiDataTrigger.Conditions>
                    <MultiDataTrigger.Setters>
                        <Setter Property="Image.Source" Value="Images/white_queen.png" />
                    </MultiDataTrigger.Setters>
                </MultiDataTrigger>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding Type}" Value="{x:Static local:PieceType.Checker}"/>
                        <Condition Binding="{Binding Side}" Value="{x:Static local:Side.Black}"/>
                    </MultiDataTrigger.Conditions>
                    <MultiDataTrigger.Setters>
                        <Setter Property="Image.Source" Value="Images/blackCheker.png" />
                    </MultiDataTrigger.Setters>
                </MultiDataTrigger>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding Type}" Value="{x:Static local:PieceType.Checker}"/>
                        <Condition Binding="{Binding Side}" Value="{x:Static local:Side.White}"/>
                    </MultiDataTrigger.Conditions>
                    <MultiDataTrigger.Setters>
                        <Setter Property="Image.Source" Value="Images/whiteChecker.png" />
                    </MultiDataTrigger.Setters>
                </MultiDataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition ></RowDefinition>
            <RowDefinition Height="472"></RowDefinition>
            <RowDefinition ></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="472"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Border Grid.Row="1" Grid.Column="1" BorderBrush="DarkRed" BorderThickness="1">
            <Grid>
                <Grid.Background>
                    <ImageBrush ImageSource="Images/gameTableImage.png"></ImageBrush>
                </Grid.Background>
            </Grid>
        </Border>
        <!--     <ItemsControl Name="ChessBoard"> -->
    <!--     <ItemsControl.ItemsPanel> -->
    <!--         <ItemsPanelTemplate> -->
    <!--             <Canvas Width="8" Height="8" Background="{StaticResource Checkerboard}"/> -->
    <!--         </ItemsPanelTemplate> -->
    <!--     </ItemsControl.ItemsPanel> -->
    <!--     <ItemsControl.ItemTemplate> -->
    <!--         <DataTemplate> -->
    <!--             <Grid Width="1" Height="1"> -->
    <!--                 <Image Width="0.8" Height="0.8" Style="{StaticResource ChessPieceStyle}" />                      -->
    <!--             </Grid> -->
    <!--         </DataTemplate> -->
    <!--     </ItemsControl.ItemTemplate> -->
    <!--     <ItemsControl.ItemContainerStyle> -->
    <!--         <Style> -->
    <!--             <Setter Property="Canvas.Left" Value="{Binding Pos.X}" /> -->
    <!--             <Setter Property="Canvas.Top" Value="{Binding Pos.Y}" /> -->
    <!--         </Style> -->
    <!--     </ItemsControl.ItemContainerStyle> -->
    <!-- </ItemsControl> -->
    </Grid>
</Window>
